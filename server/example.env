## Prisma + Supabase example env

# Use pooled connection string for Prisma Client (pgbouncer/pooler)
# Include sslmode=require as Supabase requires TLS
DATABASE_URL="postgresql://postgres:<PASSWORD>@<PROJECT_REF>.aws-0-<REGION>.pooler.supabase.com:6543/postgres?pgbouncer=true&connection_limit=1&schema=public&sslmode=require"

# Use direct/non-pooled connection for migrations
# Prefer internal host if resolvable from your environment; otherwise use the public non-pooled host
DIRECT_URL="postgresql://postgres:<PASSWORD>@<PROJECT_REF>.supabase.internal:5432/postgres?schema=public&sslmode=require"

# Alternative if internal host is not resolvable
# DIRECT_URL="postgresql://postgres:<PASSWORD>@db.<PROJECT_REF>.supabase.co:5432/postgres?schema=public&sslmode=require"

